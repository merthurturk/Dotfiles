#!/usr/bin/env bash
set -u
source "$(dirname "${BASH_SOURCE[0]}")/_lib.sh"
if [ "${1:-}" = "--describe" ]; then
  jq -n '{id:"doctor", summary:"Check that the whole setup is actually working",
          args:[], destructive:false,
          instances:[{label:"Run doctor", detail:"health check", args:[]}]}'
  exit 0
fi
exec "$DOT_ROOT/bin/doctor.sh" "$@"
